#version 330 core

in vec3 Normal;

out vec4 FragColor;

uniform vec3 objectColor;
uniform vec3 lightColor;

void main()
{
	float factor = 0.1;
	vec3 ambient = lightColor * factor;
	FragColor = vec4(ambient * objectColor, 1.0f);
}